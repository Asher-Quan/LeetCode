/**
 * Solution
 */
public class Solution {
    public int minDeletionSize(String[] A) {
        if(A.length == 0)
            return 0;
        int len = A[0].length;
        int i = 0;
        while(i < len){
            for(int j = 0 ; j < A.length ; j++){
                
            }
        }
    }
}